import {describe, expect, test} from 'vitest'

describe('Accordion test', () => { 
    test('should add two numbers', () => { 
        expect(1+1).toBe(2);
     })
 })

 describe('Accordion test', () => { 
    test('should add two numbers', () => { 
        expect(1+1).toBe(2);
     })
 })